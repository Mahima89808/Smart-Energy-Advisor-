[server]
headless = true
address = "localhost"
port = 8501

[theme]
primaryColor = "#FF4B4B"       # accent color (buttons, sliders, links)
backgroundColor = "#FFFFFF"    # main app background
secondaryBackgroundColor = "#F0F2F6"  # sidebar, widgets
textColor = "#262730"
font = "sans serif"            # or "serif", "monospace"